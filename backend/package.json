{
  "name": "hello-saas-backend",
  "version": "1.0.0",
  "description": "Backend API for Hello SaaS multilingual app",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/local-server.js",
    "dev": "nodemon src/local-server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.400.0",
    "@aws-sdk/lib-dynamodb": "^3.400.0"
  },
  "devDependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "nodemon": "^3.0.2"
  }
}
